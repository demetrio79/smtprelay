  - name: Remove IP Address
    hosts: smtp_relay
    tasks:
      - name: "Validate if IP Address is present"
        become: yes
        become_user: root
        lineinfile:
          path: /etc/postfix/network_table
          regexp: "{{ IPADDRESS }}" 
          state: absent
          backup: yes